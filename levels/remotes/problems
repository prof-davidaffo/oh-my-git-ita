title = Problemi
cards = checkout add pull push commit-auto merge

[description]

Sia tu sia il tuo amico avete lavorato sul file e volete sincronizzarvi!

[setup yours]

echo "La rimessa delle bici dovrebbe essere ???" > file
git add .
git commit -m "initial"

git push -u friend main

echo "La rimessa delle bici dovrebbe essere verde" > file

[setup friend]

git checkout main

echo "La rimessa delle bici dovrebbe essere blu" > file
git commit -a -m "friends version"

[win]

# Fai il commit delle modifiche locali.
test "$(git status -s)" = ""

[win friend]

# Guarda la proposta del tuo amico, trova un compromesso e pushalo indietro.
git rev-parse main^ && test "$(git rev-parse main^1^)" = "$(git rev-parse main^2^)"
