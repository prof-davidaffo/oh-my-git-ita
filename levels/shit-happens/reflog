title = Torna a dove eri prima
cards = checkout reflog

[description]

Metti che stavi guardando qualcosa nel passato e poi sei tornato al branch main.

Ma poi ti sei davvero distratto e dopo la pausa pranzo non ricordi su quale commit del passato eri prima. Come puoi scoprirlo?

C'e' un comando comodo che ti mostra tutti i posti in cui HEAD ha puntato in passato:

    git reflog

[setup]

for i in {1..10}; do
    git commit --allow-empty -m $i
    git branch $i
done
git checkout 3
git checkout main

[win]

# Find out where you've been before, and go back there!
test "$(git rev-parse HEAD)" = "$(git rev-parse 3)"
