title = Dividere un commit!
cards = checkout commit reset-hard reset add rebase-interactive rebase-continue show

[description]

Qui entrambe le modifiche sono finite in un solo commit! Dividile in due commit.

[setup]

echo qualcosa > file1
echo qualcos'altro > file2
git add .
git commit -m "Initial commit"

echo questo dovrebbe succedere prima >> file1
echo e questo dovrebbe succedere dopo >> file2
git commit -am "Both together"

echo questa e' un'altra modifica >> file1
echo questa e' un'altra modifica >> file2
git commit -am "Something else"

[win]

test "$(git diff-tree --no-commit-id --name-status -r main^)" = "M	file2" &&
test "$(git diff-tree --no-commit-id --name-status -r main~2)" = "M	file1"
