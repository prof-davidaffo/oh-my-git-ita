title = Fai un commit \o/
cards = add reset checkout commit

[description]

Per esercizio, fai un commit in cui tutti i file contengono una "x"!

[setup]

echo a > a
echo x > b
echo x > c
git add .
git commit -m "Initial commit"
echo x > a
echo b > b
git add b
echo c > c

[win]

# Il file a contiene "x" nell'ultimo commit di main.
test "$(git show main:a)" = x
# Il file b contiene "x" nell'ultimo commit di main.
test "$(git show main:b)" = x
# Il file c contiene "x" nell'ultimo commit di main.
test "$(git show main:c)" = x
