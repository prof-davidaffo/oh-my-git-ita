title = Ottenere l'ultima versione
cards = checkout-file

[description]

Hai lavorato al tuo tema per un po'. Ma... uff! Ora il tuo gatto cammina sulla tastiera e "ti aiuta", quindi e' tutto un disastro! :/

Ma Git e' qui per aiutarti! Per scartare tutte le modifiche fatte dal gatto e tornare alla versione dell'ultimo commit, usa `checkout`!

[setup]

echo "A" >> essay.txt
git add .
git commit -m "Initial commit"

echo "B" >> essay.txt
git commit -a -m "Improved version"

echo "C" >> essay.txt
git commit -a -m "Even better version"

echo "D" >> essay.txt
git commit -a -m "Marvelous version"

echo "blarg
blaaaargh" > essay.txt

[win]

# Restore the version from the last commit.
cat essay.txt | grep D
