title = Sandbox con un remote
cards = checkout commit-auto pull fetch push

[description]

Ecco un sandbox con un remote! Prova a fare pull, fetch o push!

Come puoi pushare tag e branch su un remote? E come puoi eliminarli di nuovo?

[setup yours]

echo "Riga 1" > tema
git add .
git commit -m "Commit iniziale"

git push -u friend main

[setup friend]

git checkout main
echo "Riga 2" >> tema
git commit -am "Un'altra riga"
