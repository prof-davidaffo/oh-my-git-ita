title = Ignorare file

[description]

Quel pollo corre in giro di continuo e cambia spesso. Non vogliamo averlo nei nostri commit.

Aggiungilo al file .gitignore e prova a usare `git add .`!

[setup]

touch .gitignore
echo important > important
git add important
git commit -m "Initial commit"

[actions]

echo "$RANDOM" > chicken
